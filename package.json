{
  "name": "immo24-address-decoder",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist web-ext-artifacts",
    "build": "node scripts/build.mjs",
    "zip:chromium": "cd dist/chromium && zip -r ../immo24-chromium.zip .",
    "zip:firefox": "cd dist/firefox && zip -r ../immo24-firefox.zip .",
    "build:all": "npm run clean && npm run build && npm run zip:chromium && npm run zip:firefox"
  },
  "devDependencies": {
    "cpy": "^11.1.0",
    "esbuild": "^0.23.0",
    "rimraf": "^6.0.1"
  }
}
